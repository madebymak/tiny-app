<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
  <% include ../views/partials/_header %>

</head>
  <body>
    <ul>
       <% Object.keys(urls).forEach(function(key) { %>
        <li><a href="<%= urls[key] %>"><%= key %></a> --->
        <%= urls[key] %>
        <a href="/urls/<%= key %>">Edit</a>

        <form method="POST" action="/urls/<%= key %>/delete">
        <input type="Submit" name="Delete" value="Delete"></form>

         <br></br></li>
      <% }); %>
    </ul>
    <a href ="http://localhost:8080/urls/new">Click here to shorten your links</a>
  </body>
</html>